

The project is closed

the todo list for next projects
    - improve validations for inputs
    - improve container registry login validation
    - improve tag and push operation
    - add more commands to encapsule docker operations
    - add ci/cd pipelines
    - add local security scanning
    - do some more test, find issues, the litle one fixe it, the mayor for a todo version
add the documentation for the project and technical memory
